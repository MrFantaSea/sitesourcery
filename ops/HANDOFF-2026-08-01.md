# Handoff — Codex takes technical lead (2026-08-01)

Zack remains product owner. This document closes the in-flight Abracadabra
correction at a clean checkpoint. After this commit the prior assistant
stops writing to this worktree unless Codex or Zack gives one explicit,
bounded assignment. `ops/CONTINUITY.md` is the canonical rulings ledger —
read it first; this file is the delta and the state of the tree.

## The correction that was finished

**The rooms obey the money.** $5 is and stays Abracadabra: gold hero,
small gold **PAID** coin, "Your page.", download + style kit unlocked.
Only $25/month is Alakazam: emerald-metal hero (same pour recipe as the
gold), green **ACTIVE** coin, live gate state. A paid/live tab with no
work lands in the empty room ("Your page comes first.") with one door
into the wizard — never on the wizard itself.

## Changed files (whole correction arc, oldest first)

Commits `645c534..0b1fcf8` on branch `build/sitesourcery-v2-20260730`:

- `645c534` style kit moved to the Preview room; attestation digest covers claims only
- `58b8e7d` keep room + per-tab persistence surviving the Stripe round-trip; address hunt mounted in the gate
- `792c4eb` teaser wording: "$5 download and account creation" (founder dictation)
- `9ca396f` empty Alakazam room; boot-landing race fix; autonomy copy purge (app scope)
- `7b2cd50` account before pay, one sweep; no download without an account (browser-bridge account)
- `7b7ac54` rooms split by money; coins replace the chip; kit applies in place; two rendering bugs fixed
- `0b1fcf8` soft/sharp/ornate rebuilt to read distinctly

Files: `abracadabra/app/index.html`, `abracadabra-app.js`,
`abracadabra-app.css`, `abracadabra-paid-download.js`,
`abracadabra-compiler.js`, **`abracadabra-account.js` (new)**;
`scripts/hosted-truth/manifest.mjs` (reseals only);
`domains/domain-search.js` is reused unmodified (one truth).

## Tests run

- `npm run check` — green: 18 live pages + 20 redirects, one shared nav,
  no dead links or resources, 28 catalog prices, 5 sellable rails, seals
  fresh, sitemap exact, canonical phone/email/rel=canonical.
- `drive-keeproom.mjs` (session scratchpad) — 10 driven journeys, all
  passing: free build + tab save; paid return lands on the page (rail
  gone, PAID coin, no phantom empty panel by **painted** display); live
  DNS address hunt; style kit one-control-at-a-time (plum → ornate →
  re-apply: still Version 1, form intact, no re-review); edit door and
  back; live room; cold free tab; cold live tab + door; download guard →
  claim panel → file; plain-URL new tab carries account, page, unlock;
  pay-sweep panel before Stripe with work mirrored.
- `drive-edges.mjs` — each edge stamps only its own markers.
- `sweep-widths.mjs` — ALL CLEAN at 320/390/480 across all 17 routes.

**Headless/structural checks are not proof of visual correctness** — two
bugs this week were invisible to green checks (hidden-attribute defeat,
Chrome fieldset legend quirk; both in the CONTINUITY incident ledger).
Rendered screenshots for this correction: `ops/handoff-shots/` (9 shots:
both rooms, empty room, free gate, centered facts, three edges + phone).
**Nothing is "done" until Zack's customer walk says so.**

## Unresolved behavior (observed, not ruled)

1. Account form asks **email only** — this is the browser-bridge
   prototype, not the real-account requirement. Zack noticed the
   thinness at the download-claim moment.
2. After claim-account creation the download fires immediately (browser
   save dialog appears at once). May deserve a beat or explicit press.
3. A fresh make is `soft` by default, so pressing Apply without changing
   anything says "That style is already on." Correct, possibly surprising.
4. Rent tier (`your-name.sitesourcery.me`) still has **no price**
   (owner decision open); the shared domain-search copy mentions renting.
5. "I set you up personally" copy survives on the lane page and FAQ —
   scheduled for their walks, per the autonomy ruling.
6. The includes-modal and the gate both state the $5-off-first-month
   credit — dedupe judgment for the walk.
7. The browser account has no sign-out/switch UI beyond the
   `?account=reset` test key.
8. Empty-room "call me and we sort it together" is backstop voice by
   design — re-judge against the autonomy ruling on the walk.

## Corrections that bind future work

- The browser-local account (`abracadabra-account.js`, localStorage) is
  a **prototype bridge**, not the real-account requirement.
- **Do not build a parallel backend.** `server/hosted` already holds the
  held Node/Postgres platform (accounts, projects, billing, support,
  domains, publication — e.g. `commerce-v2-postgres.mjs`,
  `domain-postgres-runtime.mjs`, `cancellation-worker.mjs`). Task #21 is
  an **audit-and-integrate** of that platform.
- `ops/OPERATOR-BACKEND-SPEC.md` is a **simulated draft** — all of its
  customers, invoices, failures, and policies are fictional. It is
  requirements-seeding material only. The one decided fact lives outside
  it: account creation happens immediately before first payment.
- Do not invent prices, cancellation rules, domain policies, or product
  boundaries. Do not push, deploy, or expose a payment path for anything
  unfulfillable. Normal setup must be autonomous; Zack is the
  exception-handling backstop.

## Test keys and tooling

- `/abracadabra/app/?paid=1` · `?alakazam=1` · `?account=reset`
- Dev server: `serve-nocache.py` pattern on `127.0.0.1:8899`
  (Cache-Control: no-store — plain `http.server` let Chrome serve stale
  JS during walks). Session scratchpad tools are disposable; the
  patterns worth keeping are described in `ops/CONTINUITY.md` § Tooling.

## Git state at handoff

- Branch: `build/sitesourcery-v2-20260730`, no remote configured,
  never pushed (push gate: deploy allowlist must exclude /server/,
  /scripts/, /ops/, BUILD.md, node_modules; live sitesourcery.com still
  runs the old build).
- Working tree at this commit: clean (this handoff commit is the last).
- DAARX and System Sourcery: parked, untouched.
