# FIN-015 production-held runtime bundle

State: prepared and fail closed; no installation or service-switch authority

This bundle constructor is specific to verified-held candidate
`8e59f2e9d776dbebbb705d11fcc938beadd7b9cd`, protected held workflow
`961107c823d492457596c4f1830ddcf88b355676`, and protected FIN-015
database-upgrade control `33964f972d6b2e75dedea6301796a16483a998d9`.

It independently rebuilds and verifies candidate origin seal
`1628e2e538d39c653be2154c40deb1bccd7f8b0c38f17524dc82bcc99521630b`.
That seal binds the 120-file hosted artifact, 102-migration database authority,
Legal V7, exact environment and unit schemas, the held worker contract, and
loopback-only listeners.

The constructor accepts the retained installed production environment as a
private input. It requires the exact existing 123-assignment Download-only
approved-live Stripe name inventory and modes, including the restart-safe
credential activation receipt. It preserves those credentials without
printing or digesting them and continues to hold assessment, every Custom
payment, Alakazam, Domains, and Twilio. It rejects any Twilio registry or
secret while Twilio is held, preserves the already approved registration and
recovery mail paths, adds the exact thirteen-assignment public Legal V7 tuple,
and rebinds only the candidate paths plus the three immutable release-evidence
files. The resulting inventory has 136 assignments. Secret values and
secret-derived digests never enter the summary or repository.

Output is an exclusive mode-0700 staging directory containing eight files:
three mode-0400 evidence files, one mode-0600 hosted environment, one mode-0500
API/tenant wrapper, two mode-0400 user units, and one mode-0400 bundle receipt.
The receipt explicitly denies database migration, release installation,
service switch, public cutover, provider, payment, DNS, and retirement
authority.

Preparing this bundle is not installing it. Copying a release, writing
`/etc`, changing a unit, switching evidence, starting a service, or touching
production requires a separate exact control and action-time authorization.
