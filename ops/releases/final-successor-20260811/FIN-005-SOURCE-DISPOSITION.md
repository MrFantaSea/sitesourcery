# FIN-005 preserved-source disposition freeze

Date: 2026-08-14
State: proved

Machine-readable authority:
`FIN-005-SOURCE-DISPOSITION.json`.

## Result

The exact Git and adjacent-source inventory is classified without importing,
deleting, resetting, deploying, or activating anything. The frozen successor
was clean at implementation `59ee0dd76ee6b56eca888b23e2f52b573a80e880`,
tree `7f2fed8b8729e077b321e0c87e1c31bccdc3c3fb`.

All 117 local heads not merged into that successor appear exactly once:

- 41 patch-equivalent heads are already imported and proved;
- eight cleanup archive heads have individual path/semantic dispositions;
- 18 refs across the 17 named recent lanes are individually classified; and
- 50 other non-equivalent heads remain outside the release with no effect and
  no deletion authority.

The preservation checkpoint, both verified bundles, all three registered
worktrees, the detached baseline ref, standalone repositories, dirty overlays,
and prototypes remain preserved. No archive is safe or authorized to delete.

## Bounded import completed

FIN-005B adapted the two useful donor proof paths from
`25d1026a4abbe1a6c43e3d33f6e996cd7045ef2e` to the current 87-migration
system. The joined real-PostgreSQL journey now proves assessment payment,
report and credit, Custom initial payment, one paid change, final payment,
immutable handoff, purpose accounting, provider-readback partial reversal,
and held customer notification reservations with zero dispatch claims.

The donor's self-asserted browser steps and unrelated cross-tenant operator
mail were rejected. The adapted browser helper truthfully proves only the
three real Abracadabra artifact validators at 390x844. During the joined proof,
the retained notification repository exposed a real timestamp-precision bug:
JavaScript had rounded a PostgreSQL source timestamp before the exact outbox
insert. Implementation `59ee0dd` now keeps the source timestamp in PostgreSQL,
checks reservation ordering in PostgreSQL, and proves the same-millisecond
microsecond edge creates no mail row.

The one retained release-authority requirement remains assigned, not missing:
FIN-009/010 must port the donor's owner-signature and atomic one-shot finalizer
invariants into the current exact-candidate staging/cutover authority. The
obsolete V3 Pages workflow will not be restored wholesale.

The 12 absent Domain files are not a missing product slice. The donor builds a
second, unmounted customer/operator UI and a parallel ten-route backend. Its
browser proof renders fixtures without calling that backend, and its purchase
authority accepts `exclusive_calculated_at_checkout`, which conflicts with the
later owner-selected `disabled_by_owner` tax state. The successor already has
the fuller customer Domain storefront, browser API, PostgreSQL runtime,
provider-route/lifecycle repositories, lifecycle worker, HTTP routes, and
operator work queue. Copying the donor would create another half-connected
system. Truthful held-root composition of the existing system remains assigned
to FIN-006.

The v46 service divergence is explicitly superseded: its migration is an exact
blob match, while the current implementation preserves final-payment behavior
and corrects the donor's tax, digest-order, receipt-binding, and lifecycle
authority defects.

## Proof

- focused notification unit/static: 8 passed, 0 failed;
- empty PostgreSQL 16: all 87 migrations and existing proof journeys passed;
- notification PostgreSQL regression: 1 passed, 0 failed;
- joined Assessment/Custom PostgreSQL and artifact-browser proof: 1 passed,
  0 failed;
- cumulative `npm test`: passed under exact Node 24.18.0, including 877 Node
  checks, 990 hosted passes with 14 intentional PostgreSQL skips, 205
  operations checks, both artifacts, and 15 hosted routes at three viewports;
- both disposable proof databases were removed and no browser process remained.

No public, provider, DNS, HQ, Dell, protected-database, deployment, or cutover
effect occurred.

## Ordered continuation

FIN-006 is next: freeze and prove truthful root composition across the already
integrated capabilities. Existing Domain runtime composition belongs there.
FIN-007 owns the retained UX/catalog/legal donors, and FIN-009/010 own the
retained exact-candidate finalizer invariants.

No further broad branch mapping is authorized unless the Git inventory changes.
The JSON ledger is the resume and deduplication boundary for later terminals.
