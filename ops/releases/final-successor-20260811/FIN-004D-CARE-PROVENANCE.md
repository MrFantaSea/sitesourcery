# FIN-004D Care provenance

State: proved core, commerce, repositories, and leaf surfaces; central mount remains open  
Donor: `a81d1438fd57e62e44b917c803988301945ef2ef`  
Parent integration commit: `dd6cccb`

## Imported donor scope

- the four Care wiring notes under `ops/`;
- `care-core.mjs`, `care-core-postgres.mjs`;
- Care commerce catalog, memory repository, PostgreSQL repository, and service;
- Care customer/operator surface service, PostgreSQL adapter, and HTTP adapter;
- Care browser assets under `abracadabra/app/`;
- both reusable PostgreSQL proof modules under `server/data-plane/tests/`;
- all corresponding Care unit, repository, HTTP, UI, and browser tests.

Every imported path is a byte-exact donor blob.

## Repaired database verifier contract

The donor's Care PostgreSQL proof modules were imported and explicitly called
by the clean empty-database verifier. Their exact assertion and persisted-row
counts are emitted; the surrounding broken donor verifier was not imported.

## Capability state

- exact contract, period, primary/overlap scope, ticket, and capacity ledgers;
- held catalog with exact known Care/repair/management commercial authority and
  explicit owner-redline gaps for unresolved offers;
- held quote, invoice reservation, reservation event, cancellation, ambiguity,
  eligibility, and mail-reservation flows;
- customer and operator dashboard/command projections with digest-only
  assessment references and existing management/support capability checks;
- responsive browser panels at 320, 390, and 1440 widths;
- provider, payment, mail-delivery, and customer effects remain false/held.

The canonical owner commercial catalog still governs final price convergence in
FIN-007. Importing this held Care catalog does not resolve the known version and
price projection conflict or authorize public sale.

## Proof

- focused Care tests: 46/46 passed, including three real browser viewports;
- isolated PostgreSQL 16 verifier applied all 77 migration files;
- Care core proof: 16/16 assertions, 2 contracts, 3 periods, 2 tickets, and 3
  capacity entries;
- Care commerce proof: 11/11 assertions, 2 quotes, 2 reservations, 4 reservation
  events, and 6 Care commands;
- verifier proved provider/payment/mail/customer effects false and its random
  database absent;
- PostgreSQL was stopped, port 55447 closed, and its explicit temp directory
  removed;
- complete `npm test`: passed, including deterministic artifacts and the
  15-route by 3-viewport browser audit;
- `git diff --check`: passed.

## Deferred — still required

Care central hosted composition, root HTTP mount, lifecycle worker, Stripe
billing/reconciliation, transactional mail delivery, cancellation, operator UI
composition, and final commercial catalog convergence remain required. No live
Care sale, invoice, mail, provider, or customer effect occurred.
