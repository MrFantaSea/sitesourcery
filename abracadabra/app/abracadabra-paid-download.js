(function () {
  "use strict";

  /**
   * Archived browser-download prototype.
   *
   * This file is intentionally absent from every public and hosted artifact.
   * It recognizes no URL or browser-storage marker, unlocks no controls, and
   * creates no Download. Provider-confirmed Download remains server-owned.
   */
  Object.freeze({ held: true, executableAuthority: false });
}());
