(function () {
  "use strict";

  /**
   * Archived browser-account prototype.
   *
   * This file is intentionally absent from every public and hosted artifact.
   * It retains no executable account, persistence, payment, or entitlement
   * behavior. The released account path is authenticated and server-owned.
   */
  Object.freeze({ held: true, executableAuthority: false });
}());
