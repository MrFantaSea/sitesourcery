# Data-plane provenance

This directory was imported byte-for-byte on 2026-07-28 from the disconnected
workspace package at:

```text
/Users/fantaseamac/sitesourcery-data-plane
```

The source package did not include Git metadata, so there is no source commit to
record. The import commit in the Site Sourcery repository is the provenance
anchor. `README.md` and the original PostgreSQL/D1 layout are preserved.

PostgreSQL under `supabase/migrations/` is the hosted production authority. D1
under `d1/` remains a portability lane validated through its SQLite emulator; it
is not the hosted system of record.

Changes made after import must be isolated in later commits so the source
snapshot remains auditable.
